#include <stdio.h>

/**
 * main-Serves a the entry point for the program 
  * Return: By defualt returns zero if no errors otherwise returns non-zero valu */
	int main(void) 
{	
	puts("\"Programming is like buildinga multilingual
puzzle");
	return (0);
}
