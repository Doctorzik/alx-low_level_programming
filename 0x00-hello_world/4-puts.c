#include <stdio.h>

/**
 * main-Serves a the entry point for the program 
 *
 * Return: By defualt returns zero if no errors otherwise returns non-zero value.
 */
int main(void) 
{
	puts("\" Programming is like buildinng a mulitilingual puzzle");
         return(0);
}
